[project]
name = "modal-qwen-3-5-9b"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.3.5",
]

[tool.pyright]
typeCheckingMode = "off"

[dependency-groups]
dev = [
    "openai>=2.24.0",
]
